Em outros projetos similares que trabalhei que eram parecidos com o teste, eu criei um use case master
chamado converssation flow, onde a llm responndia em forma de stream usando yield ao invés de return.

Este use case tambem era responssável por gerar um título para o chat, com base na mensagem inicial, caso não tivesse

outro ponto importante é quee o system prompt era montado em cada chat dinamicamente neste use case.
isso proporcionva uma experiência única por cliente.